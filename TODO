Replace Bootstrap with Pure.css or something similar (http://purecss.io/buttons/)

Add sentiment analysis (https://www.npmjs.com/package/sentiment)

Add status bar that shows number of rows and allows to jump to a specific row

Add warning for Safari users that download is not going to work

Add 'Reply To' column

Add a human readable time (GMT)

Add persistent logs to server

Move Progress section underneath toolbar

Add Gradient (light to dark) to toolbar

use #f3f3f3 background instead of #efefef
